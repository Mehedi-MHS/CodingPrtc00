
//Its not working properly.There is a problem :(
function add(a:number,b:number): number{
  return a+b;
};
let add1=add(10,15); //if you add anything without number,program will show error
console.log(add1);

let Person={
	name: string,
	age:number,
	interests:Array<string>
}
Person={
	name:"Abul basar",
	age:75,
	interests:["cricket","footballl"]
}
console.log(Person.name);
