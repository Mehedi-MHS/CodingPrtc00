IndexedDB
=============
/*Some naming conventions:
 * database table -> object store
 * table fields -> StoreIndex
 * primary key -> keypath
 * query method: store.add(adds value to database but donot replace existing file . if value already exists, it throws error,
                store.put() is same as add() but it reaplces existing value. it's useful for updating data)
 */


