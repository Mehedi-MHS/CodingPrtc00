//String.match(regex);
//extracts the exact match string

let str = "Hello world";
let regex = /Hello/;
let result = str.match(regex);
console.log(result);
//[ 'Hello', index: 0, input: 'Hello wo
//rld', groups: undefined ]
