//regex.test(string) ;
//checks a pattern exists or not within a string
//test() returns true,false
//Quotation is no needed inside regular expressions
let str = "Hello world";
let regex = /world/;
console.log(regex.test(str));
//true
